<h2>Merchant: <%= @merchant.username %> </h2>

<h4>Profile</h4>
<ul>
  <li>
    <strong>ID:</strong> <%= @merchant.id %>
  </li>

  <li>
    <strong>Email:</strong> <%= @merchant.email %>
  </li>
</ul>

<%# TODO: Verify that these routes are correct. %>
<h4>Actions</h4>
<%= link_to "Create new category", new_category_path %>
<%= link_to "Create new product", new_product_path %>

<%# TODO: Sync up with Cathy's session authorization method %>
<%# <% return link_to "Order Fulfillment Dashboard", merchant_order_path(@merchant, @orders) if @merchant.authorized? %> 

<h4>Products</h4>
  <table>
    <thead>
      <tr>
        <th>Name</th>
        <th>Inventory</th>
        <th>Price</th>
        <th>Action</th>
      </tr>
    </thead>

    <tbody>
    <% product_collection = @merchant.products %>
    <% product_collection.each do |product| %>
      <tr>
        <td><%= link_to "#{product.name}", merchant_products_path(@merchant, product.id) %></td>
        <td><%= product.inventory %></td>
        <td><%= product.price %></td>
        <td><%= product.active %></td>
      </tr>
    <% end %>
    </tbody>
  </table>
