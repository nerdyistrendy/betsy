<%if @product.errors.any?%>
  <section class="alert__container">
    <div class="alert alert-warning">
      <ul>
        <%@product.errors.each do |column, message|%>
          <li> <%=column%>: <%=message%> </li>
        <%end%>
      </ul>
    </div>
  </section>
<%end%>

<%= form_with model: @product, url: merchant_products_path, class: "form-group" do |f| %>
  <div class="form-group">
    <%= f.label :name %>
    <%= f.text_field(:name, class: "form-control") %>
  </div>

  <div class="form-group">
    <%= f.label :description %>
    <%= f.text_area(:description, class: "form-control") %>
  </div>

  <div class="form-group">
    <%= f.label :price %>
    <%= f.text_field(:price, class: "form-control") %>
  </div>

  <div class="form-group">
    <%= f.label :inventory %>
    <%= f.number_field(:inventory, in: 0..500, class: "form-control") %>
  </div>

  <div class="form-group">
    <%= f.label :img_url, "Image URL" %>
    <%= f.text_field(:img_url, class: "form-control") %>
  </div>

  <% if @categories %>
    <div class="form-group">
      <%= f.label :categories, "Category" %>
      <%= f.select :category, Category.all.map {|c| c.name}, {}, class: "form-control", multiple: true %>
    </div>
  <% end %>

  

  <%= f.submit "Create Product", class: "button btn btn-primary form-group" %>
<% end %>